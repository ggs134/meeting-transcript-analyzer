# 회의록 분석기 환경 변수 설정
# 이 파일을 .env로 복사하고 실제 값으로 채워주세요
# cp .env.example .env

# ============================================
# MongoDB 설정
# ============================================

# 방법 1: 개별 파라미터 사용 (권장)
# MongoDB 호스트
MONGODB_HOST=localhost

# MongoDB 포트
MONGODB_PORT=27017

# MongoDB 사용자명 (인증이 필요한 경우)
MONGODB_USERNAME=your_username

# MongoDB 비밀번호 (인증이 필요한 경우)
MONGODB_PASSWORD=your_password

# 인증에 사용할 데이터베이스 (일반적으로 'admin' 또는 사용자명과 동일)
MONGODB_AUTH_DATABASE=admin

# 방법 2: URI 직접 사용 (선택사항)
# MONGODB_URI가 설정되면 위 파라미터보다 우선 사용됩니다
# MongoDB Atlas 예시: mongodb+srv://username:password@cluster.mongodb.net/database?authSource=admin
# 로컬 MongoDB 예시: mongodb://username:password@localhost:27017/database?authSource=admin
# MONGODB_URI=mongodb://username:password@host:port/database?authSource=auth_db

# 데이터베이스 이름 (회의록이 저장된 데이터베이스)
DATABASE_NAME=company_db

# 컬렉션 이름 (회의록이 저장된 컬렉션)
# Google Drive 스키마를 사용하는 경우: shared-recordings
# 변환된 컬렉션을 사용하는 경우: meeting_transcripts
COLLECTION_NAME=meeting_transcripts

# ============================================
# Gemini API 설정
# ============================================

# Google Gemini API 키
# 발급 방법: https://makersuite.google.com/app/apikey
# 또는: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Gemini 모델 (선택사항, 기본값: gemini-2.0-flash)
GEMINI_MODEL=gemini-2.0-flash

# ============================================
# 선택적 설정
# ============================================

# 분석 결과를 저장할 컬렉션 이름 (선택사항)
# OUTPUT_COLLECTION_NAME=meeting_analysis

