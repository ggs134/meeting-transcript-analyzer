# 팀 전체 성과표 생성 가이드

## 🎯 목적

50개 이상의 회의록을 분석하여 **팀 전체의 종합 성과표**를 자동으로 생성합니다.

## 📊 생성되는 리포트

### 1. 요약 리포트
각 팀원별로:
- 참여 회의 수
- 총 발언 횟수 및 단어 수
- 평균 기여도
- 제안한 아이디어
- 완료한 업무
- 계획 중인 업무

### 2. 상세 성과표 (표 형태)
```
이름      회의참여  발언횟수  단어수   평균기여도  아이디어  완료  계획  조율
김민수    45회      234회    12,500   28.5       15개      23    18    12
이지은    42회      198회    11,200   25.3       18개      19    15    8
...
```

### 3. 내보내기 파일
- **텍스트 파일** (.txt) - 전체 리포트
- **JSON 파일** (.json) - 프로그래밍 활용
- **CSV 파일** (.csv) - Excel에서 바로 열기

---

## 🚀 사용 방법

### 방법 1: 기본 실행 (가장 간단)

```bash
# 모든 회의를 분석하여 리포트 생성
python team_report/generate_team_report.py
```

이 명령어 하나로:
1. MongoDB에서 모든 회의 가져오기
2. 각 회의를 AI로 분석
3. 참여자별 데이터 집계
4. 3가지 형식으로 리포트 저장

**생성 파일:**
- `team_performance_report.txt`
- `team_performance.json`
- `team_performance.csv`

---

### 방법 2: 특정 기간만 분석

```python
# team_report/generate_team_report.py 파일 수정

# 최근 3개월만
filters = {
    'date': {'$gte': datetime.now() - timedelta(days=90)}
}

# 특정 날짜 범위
filters = {
    'date': {
        '$gte': datetime(2024, 10, 1),
        '$lte': datetime(2024, 12, 31)
    }
}

# 특정 프로젝트만
filters = {
    'project': 'ProjectX'
}
```

---

### 방법 3: Python 코드로 직접 사용

```python
from team_report.generate_team_report import TeamPerformanceReport
from meeting_performance_analyzer import MeetingPerformanceAnalyzer

# 또는 team_report 디렉토리 내에서 실행하는 경우:
# from generate_team_report import TeamPerformanceReport

# 1. 분석기 생성
analyzer = MeetingPerformanceAnalyzer(
    gemini_api_key="YOUR_API_KEY",
    database_name="company_db",
    collection_name="meeting_transcripts",
    mongodb_host="localhost",
    mongodb_port=27017,
    prompt_template="default",
    template_version="1.0",  # 특정 버전 사용 (선택사항)
    model_name="gemini-2.0-flash"  # 모델 선택 (선택사항)
)

# 2. 리포트 생성기 생성
report = TeamPerformanceReport(analyzer)

# 3. 회의 분석 (50개든 100개든 자동 처리)
results = report.analyze_multiple_meetings()

# 4. 리포트 출력
print(report.generate_summary_report())
print(report.generate_detailed_table())

# 5. 파일 저장
report.save_full_report("my_team_report.txt")
report.export_to_json("my_team_data.json")
report.export_to_csv("my_team_data.csv")
```

---

## 📊 분석 결과 구조

팀 리포트 생성에 사용되는 개별 회의 분석 결과 구조:

```python
{
    "meeting_id": "507f1f77bcf86cd799439011",
    "meeting_title": "주간 팀 회의",
    "meeting_date": "2025-11-17",
    "participants": ["김민수", "이영희", "박철수"],
    "analysis": {
        "status": "success",
        "analysis": "AI 분석 결과...",
        "participant_stats": {  # 팀 리포트 집계에 사용
            "김민수": {
                "speak_count": 15,
                "total_words": 234,
                "timestamps": ["00:01:23", ...],
                "statements": ["발언 내용1", ...]
            },
            ...
        },
        "total_statements": 45,
        "template_used": "default",
        "template_version": "1.0",
        "model_used": "gemini-2.0-flash",
        "timestamp": "2025-11-17T10:30:00"
    }
}
```

**팀 리포트 생성 시 활용:**
- `participant_stats`: 각 팀원의 발언 횟수, 단어 수 집계
- `total_statements`: 전체 회의 발언 수
- `analysis['analysis']`: AI 분석 내용에서 아이디어, 완료 업무, 계획 추출

## 📈 출력 예시

### 콘솔 출력

```
======================================================================
📊 팀 전체 성과 요약 리포트
======================================================================

생성 일시: 2024-11-21 10:30:00
분석 팀원 수: 5명

======================================================================
👥 참여자별 요약
======================================================================

──────────────────────────────────────────────────────────────────────
👤 김민수
──────────────────────────────────────────────────────────────────────

📊 참여 통계:
  • 참여 회의: 45회
  • 총 발언 횟수: 234회
  • 총 발언 단어 수: 12,500개
  • 회의당 평균 발언: 5.2회
  • 평균 기여도: 28.5/100

💡 제안한 아이디어: 15개
  1. AI 챗봇 기능 도입으로 고객 문의 자동화...
  2. 데이터 파이프라인 최적화로 처리 속도 50% 개선...
  3. 마이크로서비스 아키텍처 전환 제안...
  ...

✅ 완료 보고: 23건
  1. Q4 제품 로드맵 수립 완료...
  2. 성능 테스트 및 벤치마크 완료...
  ...

📋 수행 예정: 18건
  1. 베타 테스트 진행 (11월 말까지)...
  2. API 문서화 작업...
  ...

──────────────────────────────────────────────────────────────────────
👤 이지은
──────────────────────────────────────────────────────────────────────
...
```

### CSV 파일 (Excel에서 열기)

| 이름 | 참여회의수 | 총발언횟수 | 총단어수 | 평균기여도 | 아이디어수 | 완료업무수 | 계획업무수 | 조율업무수 |
|------|-----------|-----------|---------|-----------|-----------|-----------|-----------|-----------|
| 김민수 | 45 | 234 | 12500 | 28.5 | 15 | 23 | 18 | 12 |
| 이지은 | 42 | 198 | 11200 | 25.3 | 18 | 19 | 15 | 8 |
| 박준호 | 40 | 187 | 10800 | 22.1 | 12 | 17 | 14 | 10 |

---

## 🎯 활용 방안

### 1. 정기 리포트 생성

```bash
# 매주 월요일 오전에 자동 실행
# cron: 0 9 * * 1 python team_report/generate_team_report.py

# 또는 배치 스크립트로
python team_report/generate_team_report.py
# 생성된 파일을 이메일로 전송하거나 Slack에 공유
```

### 2. 프로젝트 종료 시 회고 자료

```python
# 특정 프로젝트의 모든 회의 분석
filters = {'project': 'ProjectAlpha'}
report.analyze_multiple_meetings(filters)

# → 프로젝트 기간 동안 각자 무엇을 했는지 한눈에
```

### 3. 개인별 상세 리포트 추출

```python
# JSON 파일에서 특정 인물 데이터만 추출
import json

with open('team_performance.json', 'r') as f:
    data = json.load(f)

my_data = data['participants']['김민수']
print(f"내가 참여한 회의: {my_data['total_meetings']}개")
print(f"내가 제안한 아이디어: {len(my_data['ideas'])}개")
```

### 4. Excel에서 차트 생성

```
1. team_performance.csv 파일을 Excel에서 열기
2. 데이터 → 피벗 테이블 생성
3. 차트 삽입 → 막대 그래프, 원형 차트 등
4. 시각화하여 발표 자료 작성
```

---

## ⚙️ 고급 설정

### 필터링 옵션

```python
# 1. 날짜 범위
filters = {
    'date': {
        '$gte': datetime(2024, 10, 1),
        '$lte': datetime(2024, 12, 31)
    }
}

# 2. 특정 팀만
filters = {
    'team': '개발팀'
}

# 3. 특정 프로젝트만
filters = {
    'project': {'$in': ['ProjectA', 'ProjectB']}
}

# 4. 중요 회의만
filters = {
    'importance': 'high'
}

# 5. 조합
filters = {
    'date': {'$gte': datetime(2024, 10, 1)},
    'team': '개발팀',
    'importance': {'$ne': 'low'}  # 중요도가 낮지 않은
}
```

### 출력 파일명 커스터마이징

```python
# 날짜별로 파일명 생성
today = datetime.now().strftime('%Y%m%d')
report.save_full_report(f"team_report_{today}.txt")
report.export_to_json(f"team_data_{today}.json")
report.export_to_csv(f"team_data_{today}.csv")
```

---

## 🔧 문제 해결

### 문제 1: "API 할당량 초과"

**원인:** Gemini API 무료 플랜은 월 60회 제한

**해결:**
```python
# 이미 분석된 결과를 재사용
# MongoDB에 저장된 analysis 컬렉션에서 가져오기

# 또는 배치로 나눠서 분석
# 오늘 10개, 내일 10개...
```

### 문제 2: "메모리 부족"

**원인:** 50개 회의를 한번에 처리하면 메모리 부족 가능

**해결:**
```python
# team_report/generate_team_report.py 수정
# 배치 처리 추가

# 10개씩 나눠서 처리
batch_size = 10
for i in range(0, 50, batch_size):
    filters = {'_id': {'$in': meeting_ids[i:i+batch_size]}}
    report.analyze_multiple_meetings(filters)
```

### 문제 3: "분석 시간이 너무 오래 걸림"

**원인:** 50개 회의 × 각 5분 = 250분 (4시간)

**해결:**
```bash
# 백그라운드에서 실행
nohup python team_report/generate_team_report.py &

# 또는 미리 분석해두고 집계만
# 1단계: 평소에 회의 하나씩 분석 (자동화)
# 2단계: 필요할 때 집계만 빠르게
```

---

## 📊 결과 해석 가이드

### 평균 기여도 점수 의미

- **25-30점**: 높은 기여도, 적극적 참여
- **20-25점**: 보통, 정상적 참여
- **15-20점**: 낮은 편, 참여 독려 필요
- **15점 미만**: 매우 낮음, 확인 필요

**주의**: 단순 점수가 아닌 역할을 고려해야 합니다.
- 회의 진행자/리더는 자연스럽게 높음
- 신입이나 특정 분야 담당자는 낮을 수 있음

### 발언량 vs 기여도

발언이 많다고 기여도가 높은 것은 아닙니다:
- **높은 발언 + 높은 기여도**: 적극적이고 효과적
- **높은 발언 + 낮은 기여도**: 발언은 많지만 핵심 기여 적음
- **낮은 발언 + 높은 기여도**: 적은 발언이지만 임팩트 있음
- **낮은 발언 + 낮은 기여도**: 참여 독려 필요

---

## 💡 추가 팁

### 1. 정기적으로 실행하기

```bash
# 주간 리포트 자동화
# crontab -e

# 매주 월요일 오전 9시
0 9 * * 1 cd /path/to/project && python team_report/generate_team_report.py

# 매월 1일
0 9 1 * * cd /path/to/project && python team_report/generate_team_report.py
```

### 2. Slack으로 자동 공유

```python
# team_report/generate_team_report.py 마지막에 추가

import requests

def send_to_slack(message, webhook_url):
    requests.post(webhook_url, json={'text': message})

# 리포트 생성 후
summary = report.generate_summary_report()
send_to_slack(summary, SLACK_WEBHOOK_URL)
```

### 3. 대시보드 연동

```python
# JSON 데이터를 웹 대시보드에 연동
# Grafana, Metabase 등과 연결 가능

import requests

with open('team_performance.json', 'r') as f:
    data = json.load(f)

# API로 전송
requests.post('https://dashboard.company.com/api/team-data', json=data)
```

---

## 🎯 요약

### 가장 간단한 사용법

```bash
# 1. 환경 변수 설정 (.env 파일)
GEMINI_API_KEY=your-api-key

# 2. 실행
python team_report/generate_team_report.py

# 3. 결과 확인
# outputs/ 폴더에 3개 파일 생성됨
```

### 시간 예상

- 회의 1개 분석: 약 30초 ~ 1분
- 50개 회의: 약 25~50분
- 집계 및 리포트 생성: 1~2초

**총 소요 시간: 30~50분**

---

이제 팀 전체의 성과를 한눈에 파악하세요! 📊
